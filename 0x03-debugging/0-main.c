#include "main.h"

/**
 * positive_or_negative - checks for a negative or positve number
 * @i: character to check
 * Return: char status (positive or negative)
 */

void positive_or_negative(int i)
{
	if (i == 0)
	{
		printf("%d is zero\n", i);

	}
}
