#include <stdio.h>
/**
 * main - Entry point
 * Program takes no input thus the param "void"
 * Return: Always 0 (Success)
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);

	return (0);
}
